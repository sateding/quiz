/* =========================================================
   НАЛАШТУВАННЯ
   ---------------------------------------------------------
   Єдине місце, куди треба вписати свої дані після того,
   як буде розгорнуто Google Apps Script (див. apps-script.gs).
   ========================================================= */

window.CONFIG = {

  /* Адреса веб-застосунку Apps Script.
     Отримується так: Розширення → Apps Script → Розгорнути →
     Новий розгорток → Тип: Веб-застосунок → Доступ: «Усі» → Копіювати URL.
     Поки поле порожнє, квіз працює в демо-режимі:
     відповіді нікуди не надсилаються, але PDF-бриф формується. */
  endpoint: '',

  /* Скільки фото-референсів дозволяємо завантажити */
  maxFiles: 12,

  /* До якої сторони стискаємо фото перед надсиланням, px */
  imageMaxSide: 1600,

  /* Контакти студії — показуються на фінальному екрані */
  studio: {
    name:  'Dina Titanova · Interior Design',
    email: '',
    phone: '',
    site:  ''
  }
};
